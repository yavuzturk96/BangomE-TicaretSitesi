@model IEnumerable<Bangom.Models.Banka>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BankaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BankaResmi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BankaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BankaResmi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BankaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BankaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BankaID })
        </td>
    </tr>
}

</table>
